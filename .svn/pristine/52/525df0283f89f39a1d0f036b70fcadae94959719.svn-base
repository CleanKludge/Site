@using CleanKludge.Api.Responses
@using CleanKludge.Api.Responses.Articles
@model CleanKludge.Api.Responses.Articles.ArticleSection

@switch (Model.Type)
{
    case SectionType.Code:
        Html.RenderPartial("Admin/Partials/CodePreview", Model);
        break;
    case SectionType.Title:
        Html.RenderPartial("Admin/Partials/TitlePreview", Model);
        break;
    case SectionType.Text:
        Html.RenderPartial("Admin/Partials/TextPreview", Model);
        break;
}
